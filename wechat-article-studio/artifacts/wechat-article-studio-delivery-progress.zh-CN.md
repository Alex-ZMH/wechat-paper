# 公众号文章工作台 R01–R15 执行进度与修改记录

> 对应计划：wechat-article-studio-delivery-plan.zh-CN.md，版本 R2，2026-09-22。
> 当前状态：R01 已完成；后续阶段实施中。本表不是历史功能的重新验收结论。

## 1. 当前摘要

- 本轮仅更新计划和记录文件。
- 已只读核对现有公众号启动器、前端相对 API 调用，以及 Video OS 的计划和实际启动脚本。
- 原有 START-WORKBENCH.cmd 已存在；新的中文入口尚未创建。
- 新的去门禁行为尚未实施，不以“文档已更新”推断代码已经变化。
- 未启动或重启服务，未联网研究，未运行 npm test / npm run check，未进行浏览器或 Word 验收。
- 执行阶段的用户数据备份尚未进行，由 R01 完成。

## 2. 阶段台账

状态用语：未开始 / 进行中 / 实现完成待验证 / 完成 / 外部验证受阻。存在代码不等于完成，不允许把未运行写成通过。

| 阶段 | 工作包 | 主要负责人 | 状态 | 实现和验证证据 |
|---|---|---|---|---|
| R01 | 基线、需求替换和数据保护 | 主智能体 | 完成 | 2026-09-22；默认数据副本已备份并解析；仓库父级显示本项目为未跟踪目录，保留全部现有文件 |
| R02 | 中文脚本主入口 | Luna / 主智能体复核 | 实现完成待真实浏览器验证 | 中文 CMD 复用原启动器，隔离 NO_BROWSER；启动定向 4/4 通过 |
| R03 | 单实例与失败行为 | Luna / 主智能体整合 | 实现完成待真实实例验证 | 互斥、服务身份和 scope 复用；隔离模拟 npm 缺失和端口冲突通过，不误杀用户进程 |
| R04 | 哈希审批移除与历史兼容 | 主智能体 | 实现完成待验证 | 保存/导出/旧 finalize 不再创建或依赖审核凭据；旧字段只读保留；旧测试断言待 R14 更新 |
| R05 | 实时研究主流程 | 主智能体 | 实现完成待验证 | 真 Codex 调用后不再强制逐页/二次 AI 审批；未实测联网；结构化定向测试部分旧规则断言待更新 |
| R06 | 来源、导入与可选核对 | Luna / 主智能体接线 | 实现完成待真实网站检查 | 逐条结果、一坏多好、同 URL 只读一次、登录/验证码页失败；来源定向 14/14；可选接口已接 |
| R07 | 当前大纲直接采用 | 主智能体 | 实现完成待界面验收 | 默认真实论点大纲可用；生成时保存并采用当前编辑；合同定向通过 |
| R08 | writer 机械限制简化 | Luna / 主智能体复核 | 实现完成待真实模型 | 空 claim 过渡段、不全覆盖通过；未知引用仍失败；writer 定向 7/7 |
| R09 | 检查降为建议 | 主智能体 | 实现完成待接口验收 | 无回执不加阻断；检查仅按需调用且不存批准；定向 5/5 |
| R10 | 批注保留、取消交付门禁 | 主智能体 | 实现完成待接口验收 | 高优先级只显示提醒；批注合并与历史定向通过 |
| R11 | 保存与下载前自动保存 | 主智能体 | 实现完成待浏览器验收 | 有修改先保存，旧响应/新编辑不下载；失败保留输入 |
| R12 | 普通 Word 交付 | 主智能体 | 实现完成待接口验收 | 标题.docx，无审核后缀；Word 定向 10/10 |
| R13 | 四步界面真实整合 | 主智能体 | 实现完成待浏览器验收 | 移除审批入口，检查可选；生成/保存/下载接真实接口 |
| R14 | 兼容和回归 | Luna 定向 / 主智能体全量 | 完成 | 旧副本隔离 API、无审批有高批注可保存/Word；最终 npm test 122/122，npm run check 退出 0 |
| R15 | 真实流程与交付 | 主智能体 | 未开始 | — |

负责人为建议分工，不表示已经派出上述实施任务。

## 3. 本轮修改记录

### DOC-01 — R2 计划与进度文档，2026-09-22

**原因：**用户要求参照 Video OS 的执行组织方式，并补充脚本直接打开的使用入口。

**文档变化：**

- 新增 artifacts/wechat-article-studio-delivery-plan.zh-CN.md：R01–R15、接口影响、旧数据兼容、阶段验收和执行提示词。
- 新增本文件：阶段台账、实施记录模板和外部阻塞登记。
- 更新根目录 EXECUTION-PLAN.md 顶部：标明由 R2 替代，旧内容作为历史参考。
- 更新 TASKS.md 顶部：指向新的待执行计划，原状态保留为已有实现记录。

**只读依据：**

- START-WORKBENCH.cmd、scripts/start-workbench.ps1。
- src/server.mjs 的启动/health/接口与 public/app.js 的相对 API、恢复存储。
- Video OS 的重构计划、打开视频工作台.cmd、scripts/open-workbench.ps1。

**Luna 只读复核与主代理判断：**现有公众号启动器已经具备项目身份、互斥和失败清理能力，复用优于复制视频脚本；不换 origin 可避免遗失未保存浏览器副本的可见性。已把测试专用 NO_BROWSER 环境可能阻止正常打开的边界纳入 R02。该复核不是启动实测。

**未发生：**未改业务代码、未修改用户数据、未执行视频项目计划、未新增运行依赖。

**验证：**文档结构、R01–R15 编号与引用检查；不记作功能测试。

## 4. 每阶段记录模板

### R01 — 2026-09-22

- 负责人：主智能体。用户行为未变；仅保护数据和确认实施边界。
- 核对计划、现有入口、`TASKS.md`、模块及测试目录；旧文档中的固定 URL、审批、批注门禁条目按 D01–D09 待替换，历史验收不改写。
- 默认 `data/workspace.json`、`research-sessions.json`、旧存档及 `research-jobs/` 已复制到 `data/backups/delivery-r01-20260922/`；工作区和研究会话副本 JSON 解析成功。测试使用不同数据目录，不用默认数据造测试。
- `git status --short` 在父级仓库显示整个本项目为 `?? ./`，另有其他未跟踪目录；不清理、不重置，也不修改旧 writer。
- 未调用模型、未执行浏览器验收。R02/R03 入口和单实例验证由 Luna 负责，主智能体复核。

### R04/R05 — 2026-09-22，待完成针对性验证

- 主智能体独占 `src/server.mjs`、工作区服务、包合同、Word 导出调用和研究 adapter。R04 移除活动内容指纹、审核回执及人工批准对保存/Word 的控制；旧 `humanApproval` 字段只保留原样，不生成新批准。旧 finalize 接口校验当前版本后只返回已有工作区并标记 deprecated，不增版本。错误引用和版本冲突仍有独立校验。
- R05 复用 Codex CLI、后台任务、取消和去重；收到可用结构化资料即存研究会话，逐页读取与独立 AI 论点核对不再强制。部分记录无效时记录 skipped，整体无可用引用则失败；不伪造日期或原文。
- 定向命令 `node --test test/codex-research.test.mjs test/pipeline.test.mjs test/workspace-service.test.mjs test/word-exporter.test.mjs`：41 项中 35 通过、6 失败。失败是旧“阻断交付/审阅稿后缀/固定摘录长度/错误码”断言，与新政策冲突；R14 更新断言并重跑，当前不可记为通过。
- 模型和浏览器未实际调用；后续 R15 单列。R06 起继续独立实现。

### R02/R03 — 2026-09-22

- Luna 独占入口文件和启动定向测试；主智能体审阅了中文 CMD 的作用域和测试边界。新增 `打开公众号工作台.cmd`，仅在其 `setlocal` 子进程中清除 NO_BROWSER，再调用同一 `scripts/start-workbench.ps1`；旧入口不变。`START-HERE.md` 主入口已更新。
- 复用已有固定回环 origin、Mutex、健康检查项目和数据 scope、隐藏启动及仅清理本次进程树。`node --test test/launcher.test.mjs` 4/4 通过；临时隔离副本模拟 npm 缺失和被其他程序占端口，不运行或结束用户服务。
- Luna 执行时 `npm run check` 通过；全量 `npm test` 104 通过、12 失败，失败属于并行改动及旧断言，非启动测试。真实浏览器打开和已运行服务复用留待 R15，当前不标完整通过。

### R07–R13 — 2026-09-22，整合验证中

- R07 主智能体修改 `argument-map` 合同、server 与 `public/app.js`：默认大纲以实际观点作为标题和论述，不产生待编辑占位；重复章节引用允许；当前编辑在生成时采用，不需要独立确认。旧 `confirmed` 仅作为内部可写状态。R07 合同测试通过，真实 writer 待 R15。
- R08 Luna 独占 writer adapter/schema/test；主智能体核对提示词和引用白名单，并删除 workspace-service 的重复非空段落绑定检查。`node --test test/codex-writer.test.mjs` 7/7 通过，空引用、部分使用论点与未知引用均覆盖；真实模型未执行。
- R09 取消内容审核回执的活动读写；历史文件不删。可选检查返回当前建议，不把模型复述作为已核实原文，也不控制保存和导出。内容检查定向 5/5 通过；接口实际调用待验。
- R10 批注与 revision 合并逻辑复用，旧高优先级报告保留提醒但不授权或阻止交付。workspace-service 定向 4/4 通过；高批注 Word 接口待验。
- R11/R12 用户点击下载时先调用现有保存，只有成功且编辑序列未变化才按返回版本请求实际 Word；后端仍要求当前保存版本。普通标题文件名，无审阅稿/正式稿审批分叉。Word 定向 10/10 通过，浏览器下载待验。
- R13 四步布局复用，移除人工定稿按钮及其脚本调用，步骤 4 改为“检查与下载”；大纲导航只负责进入写作，检查按钮仍调用真实可选接口。`node --test test/ui-recovery.test.mjs` 6/6 通过，真实界面路径待验。
- 相关合同合并命令 `node --test test/content-review.test.mjs test/pipeline.test.mjs test/codex-writer.test.mjs test/workspace-service.test.mjs` 31/31 通过；研究/任务/恢复/UI 定向 33/33 通过。以上不是外部模型或真实浏览器通过。

### R06/R14 — 2026-09-22

- R06 Luna 独占来源模块/测试，主智能体审阅其逐条 `results` 与取消/重复 URL 行为，再在 server 接 `POST /api/research/:id/source-check`，页面添加“检查来源（可选）”。一条失败不撤销会话；整理要点不标为直接引语。来源测试 14/14，通过；真实外站读取待 R15。
- R14 在 R01 备份副本上运行 `test/delivery-api.test.mjs`：原有工作区 ID 可读且不重复；隔离文章没有批准、留有未解决 high 批注仍可保存；旧版本导出 409，当前版本返回真实 PK/DOCX 与普通标题文件名。测试不触碰默认用户数据。定向来源/旧数据/UI 21/21 通过。
- 更新与新政策冲突的研究、package、Word、workspace、内容检查和四步标签断言，未跳过测试文件。最终 `npm test`：122/122 通过、退出 0；随后 `npm run check`：退出 0。自动测试不等于真实研究、writer 和浏览器验收。

复制此模板，填入实际结果；不要保留看似已通过的示例值。

```text
阶段：Rxx
开始/完成时间：
负责人和文件所有权：
用户行为变化：
修改文件与原因：
复用的已有实现：
接口/旧数据兼容变化：
定向测试命令与退出结果：
真实浏览器操作与证据：
实际模型调用结果：
文件产物绝对路径（如有）：
未验证项及原因：
外部缺口和受影响阶段：
保留用户数据的证据：
状态：
下一阶段：
```

## 5. 外部缺口登记

本轮未执行外部调用，当前没有新增已证实的缺口。历史研究失败见原验收记录，实施时核对后登记，不直接推断当前登录失效。

| 编号 | 缺少的能力或外部条件 | 失败阶段与证据 | 影响阶段 | 可以继续的工作 | 恢复所需操作 | 状态 |
|---|---|---|---|---|---|---|
| — | 尚未执行本轮外部验证 | — | — | 按计划开展本地实施 | — | 未检查 |

不得记录 API Key、令牌或认证头。代码实现完成和真实调用通过分别记录。

## 6. 最终汇总占位

- 启动脚本实际打开浏览器：未执行。
- 实时研究返回资料：未执行。
- 真实 writer 返回文章：未执行。
- 未审核且有批注时下载：未执行。
- 保存与当前版 Word 一致：未执行。
- npm test：未执行。
- npm run check：未执行。
- Word 模板：本轮尚未修改，既有视觉证据保留。
- 可交付判断：本轮仅完成计划文档，尚不能依据此记录作出实施完成判断。
