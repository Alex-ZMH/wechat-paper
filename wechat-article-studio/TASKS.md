# 公众号文章工作台任务清单

## 当前交付执行（R2）

本轮后续改造按 [R01–R15 改造与交付计划](C:/Users/ironman/Documents/公众号/wechat-article-studio/artifacts/wechat-article-studio-delivery-plan.zh-CN.md) 执行，阶段状态记录在 [执行进度与修改记录](C:/Users/ironman/Documents/公众号/wechat-article-studio/artifacts/wechat-article-studio-delivery-progress.zh-CN.md)。目标包括脚本作为用户主入口、删除哈希及额外内容审批、接通真实后端。

R01–R13 的实现正在按阶段整合；自动与真实验收状态以进度文件为准。以下是改造前的历史实现与验收记录，其中固定 URL 作为用户入口、强制审源、内容审批、人工定稿、批注门禁和“审阅稿”文件名已由新要求取代，不代表当前政策。

## 当前实现状态（不等同于用户验收）

- [x] 根目录提供 `START-WORKBENCH.cmd`：复用已运行实例，未运行时启动并等待健康检查后打开工作台。
- [x] 已核验资料模式可作为正式使用路径。
- [x] 实时研究由后台的本机 Codex CLI 执行，不设 studio 侧总时限；仅在真实完成、用户取消或本机 Codex 明确失败时结束。
- [x] 工作台地址固定为 <http://127.0.0.1:43210>。
- [x] 一键启动固定使用本项目 `data`，不受隔离测试环境的端口/数据目录变量影响；启动失败只清理本次启动的进程树。
- [x] 大纲可以从 EvidencePacket 选择部分论点并增删、排序；writer 只覆盖选中的论点，不把未选资料自行补入文章。
- [x] 实时研究接入公开原文核对和独立论点复核；期刊摘要元数据、版面空白和仅标点变化可定位后保留原文，文字、数字、单位不得模糊替换。
- [x] 修复审查后段落事件仍绑定旧对象、导致再次编辑丢失的问题；保留未保存的版本快照。
- [ ] 本轮修复后的实时研究端到端成功验收：来源重核 11/11 通过，但一条论点的地域限制缺少绑定脚注，未保存成功研究会话。

## 冻结基线（保留既有浏览器和 Word 验证）

以下能力保留既有真实浏览器、保存恢复及 Word 视觉验收记录；这些开发验证不等于用户对文章内容的人工验收。只有本轮直接影响的路径执行最小回归，不重构其他模块：

- 四步工作台与自由写作要求；
- 新旧文章隔离、已核验资料导入和来源预览；
- 大纲编辑确认、真实结构化 writer、段落证据绑定和正文编辑；
- 批注添加/解决、高优先级门禁；
- 保存、刷新、服务重启恢复和用户修改保护；
- 读者内容与内部审计字段分离；
- 当前版本 Word 导出及字体、标题、正文缩进、行距、参考文献和链接排版。

验收依据：

- `evaluation/CORE-REPAIR-20260904.md`
- `evaluation/acceptance-20260904/fresh-browser-flow-run3.json`
- `evaluation/acceptance-20260904/fresh-word-render/`

## 实时研究结论

旧 Bridge 的同步研究和审源已从工作台实时路径移除。当前路径直接调用本机已登录的 Codex CLI 联网检索，随后由 `source-verification` 逐页读取公开 HTTP/HTML/PDF 并逐字匹配摘录，再由 `content-review` 独立核对 claim、正文数字范围与来源以及逻辑、重复、文风和可读性。两者都不是用户人工验收；内容复核结果绑定当前内容指纹并持久化，文章或大纲修改后失效。无效 JSON、缺少公开链接、摘录逐字不匹配或论点未绑定来源都会如实失败，不生成文章。

2026-09-09 的“2025年中国新能源汽车出口数据”记录只能说明本机 Codex 曾返回结构化资料，不作为本轮独立来源核验或用户验收结论。

2026-09-22 的“博士工作去向调查”隔离任务曾返回结构化结果并生成 ResearchSession `research_0f264f75ff3f`，但新的 `source-verification` 发现其中一处摘录未标注删字，逐字匹配被拒绝；因此该记录不能称为独立来源核验通过。原始记录保存在隔离目录 `data/research-smoke-20260922/`，未改写用户当前工作区。

本轮真实结果见 `evaluation/IMPROVEMENTS-20260922.md`。最后一批候选修复后来源重核 11/11 通过，论点复核仍因第一条缺少地域范围脚注依据而失败；不等同于实时研究成功。独立已核验资料模式的真实 writer、浏览器与当前版导出结果在同一记录中分开列出。真正的用户人工验收只能由用户本人给出。
